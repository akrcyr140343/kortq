// Single source of truth for the app version shown in the UI.
// Bump this on each deploy so it's clear which build is live.
// Keep in step with package.json "version".
export const APP_VERSION = "1.0.0";
